/**
 * Same path as keyra.ie — proxies to https://keyra.ie/api/deployments/apps/launcher
 * for clients that use a same-origin launcher URL.
 */
export { GET, dynamic } from "@/app/api/keyra-apps-launcher/route";
